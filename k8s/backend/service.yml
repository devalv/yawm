apiVersion: v1
kind: Service
metadata:
  name: yawm-service
  namespace: yawm-project
  labels:
    app: yawm-backend
spec:
  type: NodePort
  selector:
    app: yawm-backend-app
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30800
